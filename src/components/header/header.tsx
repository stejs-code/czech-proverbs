import { component$ } from '@builder.io/qwik';

export default component$(() => {

  return (
    <header>
      <h1 class={"text-5xl font-bold text-center py-4 border-b-2 border-sky-500 text-slate-800"}>Přísloví</h1>
    </header>
  );
});
